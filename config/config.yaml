project:
  base_dir: "."

scheduler:
  tick_minutes: 5
  pipeline_timeout_minutes: 20

paths:
  input_mail_dir: "data/inbox/correos"
  input_sheets_dir: "data/inbox/sheets"
  temp_dir: "data/tmp"
  logs_dir: "logs"
